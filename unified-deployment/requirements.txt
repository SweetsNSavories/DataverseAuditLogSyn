# Core dependencies for unified deployment
# Works for: console job, Docker container, Azure Function

# HTTP client (async)
aiohttp==3.9.1

# OAuth 2.0 / Microsoft Entra ID
msal==1.25.0

# --- Sink: Snowflake (optional) ---
# Comment out if you only use Cosmos DB / ADLS Gen2 / OneLake.
snowflake-connector-python==3.6.0

# --- Sink: Azure Cosmos DB for NoSQL (optional) ---
# Required when sink.type == "cosmos".
azure-cosmos==4.7.0

# --- Sink: ADLS Gen2 / OneLake Parquet (optional) ---
# Required when sink.type in {"adls", "onelake"}.
azure-storage-file-datalake==12.14.0
pyarrow==15.0.0

# --- Azure auth (used by Cosmos AAD mode and ADLS/OneLake AAD mode) ---
azure-identity==1.15.0

# Environment variable loader (for .env files in console/container mode)
python-dotenv==1.0.0

# Azure Functions runtime (only needed if deploying as Function App)
# Safe to install for all hosts - won't activate unless function_app.py is the entry point
azure-functions==1.18.0
